/**
 * ProjectFrame Component
 *
 * This component represents a single project item, and acts as a link to the project's detail page.
 *
 * @extends Component
 */

import React, {Component} from "react";

class ProjectFrame extends Component {
  render() {
    return (
      <div>
        I'm a project frame.
      </div>
    );
  }
}

export default ProjectFrame;